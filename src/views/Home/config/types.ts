export type SmallCardProps = {
  image?: string;
  location?: string;
  distance?: string;
};
export type MediumCardProps = {
  image?: string;
  title?: string;
};
export type LargeCardProps = {
  image?: string;
  title?: string;
  description?: string;
  btnText?: string;
};
